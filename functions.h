#define FUNCTIONS_H

int calculaConvidados(int *pConvidados, int parada);

float calculaPercentual(float parcela, float total);

float calculaGarcon(float *custos);

float calculaComida(float *custos);

void compilaGastos(float orcamento);

